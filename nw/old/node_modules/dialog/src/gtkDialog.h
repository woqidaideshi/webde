#ifndef GTKDIALOG_H
#define GTKDIALOG_H

#include <gtk/gtk.h>
#include <gdk/gdk.h>

#include "dialog.h"

class Dialog : public DialogInterface {
	public:
		Dialog();
		~Dialog();
		void messageBox(int type, const char *msg);
		void propertyBox(const char *data);

	private:
};

#endif
