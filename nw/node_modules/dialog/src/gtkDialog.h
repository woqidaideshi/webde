#ifndef GTKDIALOG_H
#define GTKDIALOG_H

#include <gtk/gtk.h>

#include "dialog.h"

class Dialog : public DialogInterface {
	public:
		Dialog();
		~Dialog();
		void messageBox(int type, const std::string &msg);
		void propertyBox(const std::string &data);

	private:
};

#endif
